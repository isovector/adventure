H reorganize dispatcher organization
  * dispatcher should check room first, then globals, then generic, unhandled
    * with support to handle or continue up the chain

M make helper classes to build animations
  * maybe a separate tool, too?
M reorganize C code
  - document at the same time
M disable input
M add support for sound effects and music
M encorporate hot.pcx walk spots for hotspots
  * provide lua bindings to walk to these spots
M procedurally calculate walkspots for objects
M let hotspots be doors
  * automatically teleport to corresponding walk spot
  * change mouse cursor to arrow

L animate action bar
L clean up lua's queue library
L provide mechanism for scaling objects based on y position
L make walking paths more natural
L write utility to build dialogues
