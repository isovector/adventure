#!./moai-untz

require "src/install_meta"
mrequire "classes/room"

--------------------------------------------------

mrequire "assets/services"

local config = require "assets/config"
Room.getRoom(config.initialRoom):load()
